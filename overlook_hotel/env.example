# env.example — example .env for local development
# Do NOT commit real secrets. Copy to .env and update values locally.

DB_USER=overlook_user
DB_PASSWORD=overlook_pass
DB_NAME=overlook_hotel
DB_HOST=localhost
DB_PORT=5432

APP_JWT_SECRET=change_this_to_a_strong_secret
SPRING_PROFILES_ACTIVE=dev